#ifndef CONFIG_H
#define CONFIG_H

/**
 * 负责加载配置文件
 * 其他模块访问配置
*/
class config
{
private:
    /* data */
public:
    config(/* args */);
    ~config();
};

config::config(/* args */)
{
}

config::~config()
{
}


#endif